import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
    title: "TazaKaber - Fresh News Delivered",
    description: "Get the latest news from around the world, delivered fresh to your inbox",
    keywords: ["news", "breaking news", "world news", "technology", "business"],
};

export default function RootLayout({
    children,
}: Readonly<{
    children: React.ReactNode;
}>) {
    return (
        <html lang="en">
            <body className={inter.className}>{children}</body>
        </html>
    );
}
